/**
 * UniversalPicker.js
 * A lightweight, zero-dependency date range picker.
 *
 * @license MIT
 * @author farisulhaq
 */

import { UniversalPicker } from './core.js';

// Version is injected by rollup build via replace plugin
UniversalPicker.VERSION = '__VERSION__';

export default UniversalPicker;
